

export default class WheelStackRowData{
    constructor(
       rowIdentifier,
       columnsOrder,
       columns,
    ) {
        this.rowIdentifier = rowIdentifier;
        this.columnsOrder = columnsOrder;
        this.columns = columns;
    }
}